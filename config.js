/**
 * Travelthing — site content config
 * ----------------------------------
 * This is the ONLY file you should need to edit as real details
 * come in. Every page reads from here, so a value only has to be
 * entered once and it updates the header, footer and contact page
 * together — nothing to keep in sync by hand.
 *
 * Leave a value as null until you have the real one. The site
 * will show that channel as "coming soon" (buttons) or simply
 * keep it hidden (address / hours / email) instead of publishing
 * a fake or dead link.
 *
 * The WhatsApp number is already live and set directly in the
 * HTML/footer of each page, so it isn't listed here.
 */
window.TRAVELTHING = {
  facebookUrl: null,   // e.g. "https://facebook.com/travelthing"
  websiteUrl: null,    // your original website's URL
  email: null,          // e.g. "hello@travelthing.com"
  address: null,        // e.g. "House 12, Road 5, Dhanmondi, Dhaka"
  hours: null           // e.g. "Sun–Thu, 10:00 AM – 7:00 PM"
};
